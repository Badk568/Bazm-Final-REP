import{notFound}from"next/navigation";import EventDetail from"@/components/EventDetail";import{getPublishedEventBySlug,toPublicEvent}from"@/lib/events";export const dynamic="force-dynamic";export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const event=await getPublishedEventBySlug((await params).slug);return{title:event?.seoTitle||event?.title||"Event",description:event?.seoDescription||event?.shortSummary}}export default async function EventPage({params}:{params:Promise<{slug:string}>}){const managed=await getPublishedEventBySlug((await params).slug);if(!managed)notFound();return<EventDetail event={await toPublicEvent(managed)} specialInstructions={managed.specialInstructions} statusReason={managed.statusReason}/>}
