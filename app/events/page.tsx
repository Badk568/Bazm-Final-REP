import Nav from"@/components/Nav";import Footer from"@/components/Footer";import EventExplorer from"@/components/EventExplorer";import{getPublishedEvents,listCategories,toPublicEvent}from"@/lib/events";export const metadata={title:"Programme"};export const dynamic="force-dynamic";export default async function Events(){const events=await Promise.all((await getPublishedEvents()).map(toPublicEvent)),categories=(await listCategories(false)).map(item=>item.name);return<main><header className="page-head paper"><Nav/><div className="container"><p className="kicker">The programme</p><h1>Something worth<br/>showing up for.</h1><p>Explore what is gathering at Bazm.</p></div></header><section className="section"><div className="container"><EventExplorer events={events} categories={categories}/></div></section><Footer/></main>}
