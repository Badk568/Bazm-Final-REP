import{notFound}from"next/navigation";
import{requireStaff}from"@/lib/auth";
import{getCheckoutOrderByReference}from"@/lib/ticketing";
import{dateTime,money}from"@/lib/format";
export const dynamic="force-dynamic";
export default async function OrderDetail({params}:{params:Promise<{reference:string}>}){await requireStaff(["ADMIN","EVENT_MANAGER"]);const order=await getCheckoutOrderByReference((await params).reference);if(!order)notFound();return<><header className="stage-admin__page-head"><div><p>Order</p><h1>{order.reference}</h1></div><a href="/admin/orders">Back to orders</a></header><section className="admin-order-detail"><div><span>Customer</span><h2>{order.fullName}</h2><p>{order.email}<br/>{order.phone}</p></div><dl><div><dt>Event</dt><dd>{order.eventTitle}</dd></div><div><dt>Ticket tier</dt><dd>{order.tierName} × {order.quantity}</dd></div><div><dt>Unit price</dt><dd>{money(order.unitPrice)}</dd></div><div><dt>Total</dt><dd>{money(order.total)}</dd></div><div><dt>Status</dt><dd>{order.status.replaceAll("_"," ")}</dd></div><div><dt>Reservation expiry</dt><dd>{dateTime(order.expiresAt)} PKT</dd></div><div><dt>Created</dt><dd>{dateTime(order.createdAt)} PKT</dd></div></dl><aside><b>Payment-proof review will be added in Stage 4.</b><p>This stage provides order visibility and inventory reservations only. No approval or QR-ticket action is available here.</p></aside></section></>}
