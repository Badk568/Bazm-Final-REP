import CategoryManager from"@/components/admin/CategoryManager";import{requireStaff}from"@/lib/auth";import{listCategories}from"@/lib/events";export const metadata={title:"Categories · Bazm admin"};export const dynamic="force-dynamic";export default async function CategoriesPage(){await requireStaff(["ADMIN"]);return<><header className="stage-admin__page-head"><div><p>Programme taxonomy</p><h1>Categories</h1></div><span>Drag-free ordered controls</span></header><p className="category-intro">Categories drive the public programme filters. Assigned categories are protected from deletion.</p><CategoryManager initial={await listCategories()}/></>}
