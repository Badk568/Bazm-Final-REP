import{notFound}from"next/navigation";import EventDetail from"@/components/EventDetail";import{requireStaff}from"@/lib/auth";import{getManagedEvent,toPublicEvent}from"@/lib/events";export const dynamic="force-dynamic";export const metadata={title:"Secure event preview · Bazm",robots:{index:false}};export default async function EventPreview({params}:{params:Promise<{id:string}>}){await requireStaff(["ADMIN","EVENT_MANAGER"]);const event=await getManagedEvent((await params).id);if(!event)notFound();return<EventDetail event={await toPublicEvent(event)} preview specialInstructions={event.specialInstructions} statusReason={event.statusReason}/>}
