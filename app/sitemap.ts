import type{MetadataRoute}from"next";import{events}from"@/lib/data";export default function sitemap():MetadataRoute.Sitemap{const b=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const pages:MetadataRoute.Sitemap=["","/events","/about","/visit","/terms","/privacy","/refund-policy"].map(p=>({url:b+p,changeFrequency:p==="/events"?"daily":"monthly"}));const eventPages:MetadataRoute.Sitemap=events.map(e=>({url:`${b}/events/${e.slug}`,lastModified:new Date(e.date),changeFrequency:"weekly"}));return pages.concat(eventPages)}
