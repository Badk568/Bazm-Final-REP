"use client";import{useState}from"react";import{useRouter}from"next/navigation";export default function Lookup(){const router=useRouter(),[r,setR]=useState(""),[k,setK]=useState("");return<form className="lookup" onSubmit={e=>{e.preventDefault();router.push(`/payment/${r.trim().toUpperCase()}?key=${encodeURIComponent(k.trim())}`)}}><label className="field"><span>Booking reference</span><input required value={r} onChange={e=>setR(e.target.value)} placeholder="BZM-2026-ABC123"/></label><label className="field"><span>Secure access key</span><input required value={k} onChange={e=>setK(e.target.value)} placeholder="From your booking link"/></label><button className="button button--terra">Find booking</button><small>The reference alone is not sufficient; the private key protects your booking.</small></form>}
