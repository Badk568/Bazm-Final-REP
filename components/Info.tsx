import Nav from"./Nav";import Footer from"./Footer";export default function Info({kicker,title,intro,children}:{kicker:string;title:string;intro:string;children:React.ReactNode}){return<main><header className="info-head plaster"><Nav dark/><div className="container"><p className="kicker">{kicker}</p><h1>{title}</h1><p>{intro}</p></div></header><section className="section"><article className="policy">{children}</article></section><Footer/></main>}
